{
  "name": "caldero-envio-functions",
  "description": "Cloud Functions for caldero-envio Mapbox proxy",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^11.11.0",
    "firebase-functions": "^7.3.2",
    "mercadopago": "^3.2.0",
    "nanoid": "^3.0.0"
  },
  "scripts": {
    "deploy": "firebase deploy --only functions",
    "test": "node --test",
    "test:integration": "node --test integration/"
  },
  "devDependencies": {
    "firebase-functions-test": "^3.1.0"
  }
}
